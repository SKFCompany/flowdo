<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application>
        <!--
            Фоновая служба напоминаний.
            stopWithTask="false" — служба продолжает работать после
            того как пользователь убрал приложение из списка задач.
            foregroundServiceType="specialUse" нужен для Android 14+.
        -->
        <service
            android:name="org.flowdo.flowdo.ServiceReminder"
            android:exported="false"
            android:foregroundServiceType="specialUse"
            android:stopWithTask="false">
            <!--
                Обязательное объяснение для Android 14+ при типе specialUse.
            -->
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="reminder_notifications" />
        </service>
    </application>
</manifest>
